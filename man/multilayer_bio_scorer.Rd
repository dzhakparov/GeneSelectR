% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{multilayer_bio_scorer}
\alias{multilayer_bio_scorer}
\title{Multi-Layer Biological Scorer}
\usage{
multilayer_bio_scorer(
    genes,
    layers = c("go", "msigdb"),
    disease_term = NULL,
    target_terms = NULL,
    go_mode = "supervised",
    msigdb_categories = c("C2", "C7", "H"),
    organism = "Homo sapiens",
    use_cache = TRUE,
    verbose = TRUE,
    ...
)
}
\arguments{
\item{genes}{Character vector of gene symbols to score}

\item{layers}{Character vector, subset of \code{c("go", "msigdb")}.
Controls which evidence layers are active.}

\item{disease_term}{Character string describing the disease (e.g.,
"breast cancer", "ulcerative colitis"). Used by the MSigDB source.}

\item{target_terms}{Character vector of GO term IDs (for the GO layer)}

\item{go_mode}{Character, "supervised" or "data_driven" (for the GO layer)}

\item{msigdb_categories}{Character vector of MSigDB collections to search.
Default: c("C2", "C7", "H") = curated, immunologic and Hallmark gene
sets.}

\item{organism}{Character, species name for msigdbr. Default: "Homo sapiens".}

\item{use_cache}{Logical, cache results to disk.}

\item{verbose}{Logical, print progress.}

\item{...}{Additional arguments passed to the GO layer
(\code{biological_scorer}).}
}
\value{
Numeric vector of combined biological scores (one per gene),
percentile-normalized between 0 and 1.
}
\description{
Combines Gene Ontology semantic similarity and MSigDB gene-set membership.
Each active source contributes equally through a geometric mean.
}
\examples{
if (interactive() && requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
    multilayer_bio_scorer(
        c("TP53", "BRCA1"),
        layers = "go",
        target_terms = "GO:0006915", verbose = FALSE
    )
}

}
