% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology_network.R
\name{get_disease_seeds_opentargets}
\alias{get_disease_seeds_opentargets}
\title{Derive Disease Seed Genes from Open Targets}
\usage{
get_disease_seeds_opentargets(
    disease_term,
    max_seeds = 100,
    min_score = 0.1,
    use_cache = TRUE,
    force_refresh = FALSE,
    verbose = TRUE
)
}
\arguments{
\item{disease_term}{Disease name or EFO ID}

\item{max_seeds}{Integer, take at most this many top-scoring targets
(default: 100)}

\item{min_score}{Numeric, drop associations below this overall score
(default: 0.1). Applied after the top-N cut, so a disease with weak
associations yields fewer seeds rather than padding with noise.}

\item{use_cache}{Logical, read/write the frozen seed file (default: TRUE)}

\item{force_refresh}{Logical, ignore any cached file and submit a new
request.}

\item{verbose}{Logical}
}
\value{
Data frame with columns: ensembl_id, symbol, score. An empty data
frame indicates that the resolved disease has no qualifying associations.
}
\description{
Queries the Open Targets Platform for the top targets associated with a
disease, ranked by overall association score. Results are frozen to the
package cache keyed by (EFO ID, max_seeds, min_score) so the API is called
at most once per parameter combination - subsequent runs load from disk and
are fully reproducible offline.
}
\examples{
if (interactive()) {
    get_disease_seeds_opentargets(
        "EFO_0000274",
        max_seeds = 5, verbose = FALSE
    )
}
}
