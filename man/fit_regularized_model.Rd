% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fit_regularized_model}
\alias{fit_regularized_model}
\title{Fit Regularized Logistic Regression}
\usage{
fit_regularized_model(
  X_train,
  y_train,
  method = "elastic_net",
  alpha = 0.5,
  cv_folds = 5,
  gene_groups = NULL,
  group_penalty = "grLasso"
)
}
\arguments{
\item{X_train}{Training feature matrix (n_train x n_genes)}

\item{y_train}{Training labels (factor with 2 levels)}

\item{method}{"lasso", "elastic_net", or "group_lasso"}

\item{alpha}{Elastic net mixing parameter (ignored for group_lasso)}

\item{cv_folds}{CV folds for lambda selection}

\item{gene_groups}{Integer vector of length ncol(X_train) assigning each gene
to a group (required for group_lasso; ignored for lasso/elastic_net).
If NULL and method is "group_lasso", groups are auto-generated from
correlation clustering.}

\item{group_penalty}{"grLasso" (default), "grMCP", or "grSCAD" for group_lasso}
}
\value{
List with: selected (gene indices), coefficients (|beta|), lambda,
n_selected, cv_fit (model object), predict_fn (function: X_test -> probabilities)
}
\description{
Supports lasso, elastic net (via glmnet), and group lasso (via grpreg).
Returns a model-agnostic predict_fn so the caller doesn't need to know
which package was used.
}
\keyword{internal}
