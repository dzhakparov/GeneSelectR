% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology_network.R
\name{score_network_layer}
\alias{score_network_layer}
\title{Score Genes by Network Proximity to Disease Seeds}
\usage{
score_network_layer(
    genes,
    disease_term,
    string_score_threshold = 400,
    restart_prob = 0.5,
    max_seeds = 100,
    min_score = 0.1,
    organism = 9606,
    string_version = "12.0",
    use_cache = TRUE,
    verbose = TRUE
)
}
\arguments{
\item{genes}{Character vector of candidate gene symbols to score}

\item{disease_term}{Disease name or EFO ID (passed to Open Targets)}

\item{string_score_threshold}{Integer, minimum STRING combined score for an
edge to be included (default: 400, STRING's "medium confidence")}

\item{restart_prob}{Numeric, RWR restart probability (default: 0.5). Higher
retains more probability near the seeds; lower values allow wider
propagation.}

\item{max_seeds, min_score}{Passed to \code{get_disease_seeds_opentargets}}

\item{organism}{STRING species id (default: 9606, human)}

\item{string_version}{STRING version (default: "12.0")}

\item{use_cache}{Logical}

\item{verbose}{Logical}
}
\value{
Numeric vector (length = length(genes)) of percentile-normalised
network relevance scores between 0 and 1, named by gene.
}
\description{
Places the disease seed genes on the STRING functional interaction network
and propagates relevance via random walk with restart (RWR). Each candidate
gene receives a score equal to its stationary RWR probability - high for
genes that are seeds themselves or sit close to many seeds in the network,
low for genes far from disease biology.
}
\examples{
if (interactive()) {
    score_network_layer(
        c("IL6", "STAT3"), "EFO_0000274",
        verbose = FALSE
    )
}
}
