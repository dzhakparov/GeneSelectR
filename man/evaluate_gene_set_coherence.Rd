% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biology_semantic.R
\name{evaluate_gene_set_coherence}
\alias{evaluate_gene_set_coherence}
\title{Evaluate the Semantic Coherence of a Selected Gene Set}
\usage{
evaluate_gene_set_coherence(
    gene_set,
    background_genes = NULL,
    n_permutations = 50,
    ontology = "BP",
    sim_method = "resnik",
    organism = "human",
    use_cache = TRUE,
    random_seed = 123,
    verbose = TRUE
)
}
\arguments{
\item{gene_set}{Character vector containing the selected genes.}

\item{background_genes}{Character vector to draw the null from (typically all
candidate genes). If NULL, no null is computed.}

\item{n_permutations}{Number of random gene sets used for comparison.}

\item{ontology}{GO ontology branch (default "BP")}

\item{sim_method}{Term-pair similarity metric (default "resnik")}

\item{organism}{Organism for the GO cache (default "human")}

\item{use_cache}{Logical}

\item{random_seed}{Integer seed for the random-gene-set comparison.}

\item{verbose}{Logical}
}
\value{
List with: mean_coherence, n_annotated, expected_coherence,
empirical_p (NA if no background supplied)
}
\description{
Computes mean pairwise best-match-average GO semantic similarity among
selected genes. This downstream assessment does not change the ranking.
}
\details{
To contextualise the value, an empirical null is estimated by drawing random
gene sets of the same size from the background and computing their coherence.
}
\examples{
if (interactive() && requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
    evaluate_gene_set_coherence(c("TP53", "BRCA1"), verbose = FALSE)
}
}
