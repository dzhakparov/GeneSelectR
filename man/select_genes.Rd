% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_genes.R
\name{select_genes}
\alias{select_genes}
\title{Run the predictive GeneSelectR workflow}
\usage{
select_genes(
    x,
    outcome,
    assay_name = "log_expression",
    n_genes = 20L,
    alpha = c(0.5, 1),
    B = 50L,
    permutations = 20L,
    null_B = 20L,
    k_folds = 5L,
    seed = 123L,
    workers = 1L
)
}
\arguments{
\item{x}{A numeric matrix with samples in rows and named genes in columns,
or a SummarizedExperiment with genes in rows and samples in columns.}

\item{outcome}{A two-level factor for a matrix input, or the name of a
colData column for a SummarizedExperiment. The second level is positive.}

\item{assay_name}{Name of the assay containing normalized expression.}

\item{n_genes}{Number of genes to return from the ranking.}

\item{alpha}{Elastic-net mixing values compared using internal AUC.}

\item{B}{Number of repeated model fits for each alpha.}

\item{permutations}{Number of shuffled-outcome references.}

\item{null_B}{Number of model fits per shuffled outcome.}

\item{k_folds}{Number of stratified folds. The benchmark used five.}

\item{seed}{Random seed, restored on return.}

\item{workers}{Number of fitting workers. Default one.}
}
\value{
A geneselectr_result containing gene_scores, parameters, cv_results
and stability, with additional selected_genes and alpha_comparison fields.
selected_genes contains the first n_genes gene names from the ranking.
}
\description{
Rank genes with repeated elastic-net inclusion and excluded-sample
contribution. Biological annotations are applied downstream and do not enter
the ranking.
}
\details{
Input must already be normalized. Select variable genes and fit
preprocessing operations using training samples only. All genes receive
equal penalty weights. The selected alpha has the greatest mean internal
AUC; ties retain the first alpha supplied. The component ratios are ranking
quantities and provide no probabilities, p-values or FDR guarantee.
}
\examples{
data(asthma_example)
fit <- select_genes(asthma_example, "severity",
    n_genes = 10,
    alpha = 0.5, B = 2, permutations = 1, null_B = 2
)
head(fit$selected_genes)
}
